---
description: Vocello testing tiers, determinism, and the no-agent-screen-driving rule
globs: Tests/**,scripts/macos_test.sh,scripts/ios_device.sh,scripts/lib/test_models.sh,scripts/build_foundation_targets.sh
alwaysApply: false
---

# Testing (deterministic XCUITest, on-device iOS)

Tests are run by `scripts/*.sh` + `xcodebuild`, **never** by an agent driving the screen
(the old computer-use / desktop-MCP harness is gone). Keep tests deterministic and
self-driving. See [`AGENTS.md`](mdc:AGENTS.md) (Workflows) and the testing runbook.

## iOS (on-device only)

**Start here:** [`ios-device-testing.md`](mdc:docs/reference/ios-device-testing.md) § Visual reference
(diagrams) and § Daily workflow.

All `VocelloiOSUITests` run on a **paired physical iPhone** via `scripts/ios_device.sh`.
The MLX engine cannot initialize on the iOS Simulator — never use `platform=iOS Simulator`,
XcodeBuildMCP simulator tools, or Axiom `xcui` / simulator-tester for iOS work.

- **Warm path** — [`VocelloUITestApp`](mdc:Tests/VocelloiOSUITests/VocelloUITestApp.swift)
  launches the real engine once per session for `Smoke`, `Sheet`, and `ReviewTour`.
- **Cold / download** — `ColdGeneration` self-launches for cold-start generation tests.
  `OnDeviceDownload` is **opt-in** (`ui-test --download`) — it uninstalls `pro_custom` in setUp.
- **UI bench** — `VocelloiOSBenchUITests` (full matrix through the real Studio UI) runs
  ONLY via `scripts/ios_device.sh bench-ui`; gate `scripts/check_ios_ui_bench.py`. Its
  corpus must stay IDENTICAL to `BenchMatrixSpec.corpus`. Debug hooks
  (`iosStudio_lastGenerationComplete` / `iosStudio_generationError` /
  `iosStudio_benchClearScript`) live in
  [`IOSStudioBenchHooks`](mdc:Sources/iOS/Studio/IOSStudioBenchHooks.swift), active only
  under `QWENVOICE_UI_TEST_HOOKS=1`. Clone cells need a saved voice enrolled ON the
  phone — the iPhone mic is unavailable through iPhone Mirroring.

Default device gate (`scripts/ios_device.sh test`): Smoke + Sheet + ColdGeneration.
Requires **all three Speed models** on device (Custom + Design + Clone). OnDeviceDownload:
`ui-test --download` (separate lane; uninstalls Custom Voice).

- **Interference detection:** `scripts/ios_device.sh device-state` (exit code = verdict)
  — layered CoreDevice JSON, bundle-ID mirror host, Swift OCR classify; `--json-v2` /
  `watch` for structured sub-signals. **`MIRROR_ACTIVE` means mirror up, not phone unlocked
  for XCUITest.** Full reference: [`ios-device-probe.md`](mdc:docs/reference/ios-device-probe.md).
  Preflight, bench/gate, and ui-test retry consult it — see
  [`ios-device-testing.md`](mdc:docs/reference/ios-device-testing.md) § Interference states.
- **iOS bench-ui typing:** `VocelloiOSBenchUITests` validates composer input via
  `textInput_lengthCount` (iOS), not macOS `textInput_charCount`.

GitHub CI runs **compile-only** (`build-for-testing` with `generic/platform=iOS`); the real
pre-merge gate is `scripts/ios_device.sh gate` on your paired iPhone.

## macOS UI test drivers

macOS has **no fake-backend tier** — all `VocelloMacUITests` classes exercise real XPC.

- **Human driver** — `VocelloMacSmokeUITests`, `VocelloMacHumanJourneyUITests`,
  `VocelloMacReviewUITests` share [`VocelloMacUITestApp`](mdc:Tests/VocelloMacUITests/VocelloMacUITestApp.swift)
  (one app session, `resetToGenerateSurface()` between tests) and
  [`VocelloMacUIQuery`](mdc:Tests/VocelloMacUITests/VocelloMacUIQuery.swift) (`XCTNSPredicateExpectation`
  only — no RunLoop polling).
- **Bench driver** — `VocelloMacBenchUITests` uses
  [`VocelloMacUIBase`](mdc:Tests/VocelloMacUITests/VocelloMacUIBase.swift) (cold/warm relaunch,
  matrix navigation). Waits on `mainWindow_lastTelemetryFlushed` instead of fixed sleeps.
- **Script lanes** scope `-only-testing`: `test` → smoke; `journey` → journeys; `review` → catalog;
  `bench-ui` → `testFullMatrix` only.
- **UITest hooks** — `QWENVOICE_UI_TEST_HOOKS=1` / debug telemetry expose
  `mainWindow_composeReady_*`, `mainWindow_lastGenerationComplete`, `mainWindow_lastTelemetryFlushed`
  (see [`macos-app-guide.md`](mdc:docs/reference/macos-app-guide.md)).

## Determinism rules

- Use `waitForExistence` / `XCTNSPredicateExpectation`, **not** `usleep`/`Thread.sleep`/
  RunLoop polling, to wait for UI.
- Register `installSystemAlertMonitor()` in `setUp` so permission/automation dialogs can't
  stall a run.
- Query by stable `accessibilityIdentifier`; do not rely on brittle label/hierarchy
  fallbacks. Never let a `screen_*` container shadow descendant ids — use the
  `screenPresenceMarker(_:)` leaf marker (see [`IOSAccessibility`](mdc:Sources/iOS/IOSAccessibility.swift)).

## Real-engine work is on-device only

The iOS Simulator cannot host the MLX/Metal engine. All iOS UI tests and real generation/
download validation run on a paired iPhone via `scripts/ios_device.sh`. macOS UI smoke runs via
`scripts/macos_test.sh test` (calls `models ensure` first; exports
`QVOICE_REQUIRE_TEST_MODELS=1` so missing weights fail instead of `XCTSkip`).

## Model preflight (macOS)

- Canonical weights: `~/Library/Application Support/QwenVoice/models`.
- Debug/test context symlinks `QwenVoice-Debug/models` → canonical
  ([`scripts/lib/test_models.sh`](mdc:scripts/lib/test_models.sh)).
- `scripts/macos_test.sh models check|ensure|install` — see testing runbook §1b.
- Escape: `QVOICE_SKIP_MODEL_ENSURE=1`, `QVOICE_TEST_MODELS_NO_NETWORK=1`.

## macOS bench lanes

- **`gate`:** default = model ensure → inputs → foundation build → UI smoke → crash check.
  Opt-in bounded engine bench + baseline compare: `QWENVOICE_GATE_BENCH=1 scripts/macos_test.sh gate`
  (compares vs `benchmarks/baselines/mac-gate-bench.json`; reseed with `--save-baseline` after intentional perf changes)
  (custom/speed/medium + audioQC).
- **`profile`:** Instruments trace during in-process `vocello bench`. **Fails** when bench
  exits non-zero unless `--allow-bench-fail` or `QVOICE_MAC_PROFILE_ALLOW_BENCH_FAIL=1`
  (use when you only need the trace artifact).
- **`bench-ui`:** supplementary **XPC UI matrix** (29 takes default) through real app +
  XPC — use when Native/Services/Views/XPC changed. **CLI `vocello bench` remains primary
  for engine RTF/decode.** See [`benchmarking-procedure.md`](mdc:docs/reference/benchmarking-procedure.md) §4.10.
- **`journey`:** phase-A human flows (player + history replay) via `VocelloMacHumanJourneyUITests`.
- **`review`:** catalog captures via `VocelloMacReviewUITests` (`--subset resting|full`).
- **`uitest-doctor`:** Gate 1–3 readiness before unattended XCUITest (`automationmodetool`,
  signing, Accessibility).

See [`macos-testing.md`](mdc:docs/reference/macos-testing.md) and
[`benchmarking-procedure.md`](mdc:docs/reference/benchmarking-procedure.md) §4.8–§4.10.
