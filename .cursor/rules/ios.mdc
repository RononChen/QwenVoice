---
description: Vocello iOS app invariants (VocelloiOS / QVoiceiOS)
globs: Sources/iOS/**,Sources/iOSSupport/**
alwaysApply: false
---

# iOS app invariants

Deep role guide: [`.agents/ios-engineer.md`](mdc:.agents/ios-engineer.md). Architecture:
[`docs/ARCHITECTURE.md`](mdc:docs/ARCHITECTURE.md) §6 (iOS request lifecycle).

- **Engine runs in-process** on Metal (`NativeRuntimeFactory` → `MLXTTSEngine`, wrapped in
  `TTSEngineStore`). The iOS app deliberately does **not** link the macOS XPC stack. All
  generation and UI tests are **on-device only** — the Simulator cannot host MLX/Metal.
- **Cooperative cancel.** iOS does not conform to `ActiveGenerationCancellable` for real
  takes; the generate flow must discard the result on `Task.isCancelled` so cancelled takes
  never land in History.
- **Use `IOSScrollView`** for vertical scroll, not raw `ScrollView`.
- **Animations** route through `iosAppAnimation` (view modifier) / `IOSAccessibleAnimation.perform`
  (imperative) and honor Reduce Motion; Liquid Glass falls back to solid fills under Reduce
  Transparency. (macOS equivalents: `appAnimation` / `AppLaunchConfiguration.performAnimated` —
  `animationsEnabled` is a LIVE read since 2026-07-02.)
- **No batch on iOS** (removed 2026-07-02, maintainer decision — native engine unsupported,
  Jetsam risk). Do not re-add without a sequential-streaming design validated on device.
- **Cold launch lands Studio → Custom** (tab restores; studio mode deliberately does not —
  `8f70f68`). Explicit handoffs (Voices → Clone etc.) still switch modes in-session.
- **1.7B model variants only** (Speed 4-bit / Quality 8-bit; iPhone = Speed-only by contract).
  The 0.6B variant was ruled out 2026-07-02: Voice Design requires 1.7B.
- **Thermal:** proactive warm (prewarm/clone priming) is blocked at serious/critical thermal
  state (`TTSEngineStore`); never block generation itself on thermal without a maintainer
  decision. `QVOICE_IOS_THERMAL_GATE=off` is the escape hatch.
- **No color-only signal:** mode colors always pair with an icon/label/position cue.
- **Entitlements:** keep the App Group `group.com.patricedery.vocello.shared` and
  `com.apple.developer.kernel.increased-memory-limit`; `project.yml` assigns the right
  entitlements file per build. Respect the RAM-gated clone load profile
  (`.fullCapabilities` vs `.iOSProductionDefault`).
- **Stable `accessibilityIdentifier`s** (`screen_*`, `rootTab_*`, `textInput_*`,
  `studioChip_*`, `iosModel*`) must survive refactors — they are UI-test surface area.
