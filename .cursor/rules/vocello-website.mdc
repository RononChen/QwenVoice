---
description: Marketing website — React/Vite, not native Axiom auditors
globs:
  - website/**
---

Vocello marketing site (`website/`). Deployed via Vercel; product claims must match app contract and [`docs/reference/`](../../docs/reference/).

## Tooling

- React / Vite / npm: Context7 MCP (user rule) or Vercel plugin skills
- Build: `npm --prefix website run build`
- **Do not** run Axiom native-app auditors for website-only changes

## Product truth

Cross-check copy and Listen samples against `Sources/Resources/qwenvoice_contract.json` and [`CLAUDE.md`](../../CLAUDE.md).

Playbook: [`.cursor/rules/vocello-agent-playbook.mdc`](vocello-agent-playbook.mdc)
